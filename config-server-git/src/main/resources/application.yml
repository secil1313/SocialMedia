server:
  port: 8889
spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/emrekazanci/Java7configymlSocialMeadia.git
          default-label: main
          skip-ssl-validation: true
          username: emrekazanci
          password: ${java7configpassword}